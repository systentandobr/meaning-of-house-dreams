# Vite substitui esta variável durante o build. O Caddy do frontend encaminha
# este caminho ao serviço Go privado; não exponha a URL interna no browser.
VITE_API_URL=/gateway
# Runtime-only no serviço casa-web; use a private domain do Railway.
API_URL=https://casa-api-production-4d11.up.railway.app
